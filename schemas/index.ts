import product from './product';
import customizationOption from './customizationOption';
import siteSettings from './siteSettings';

export const schemaTypes = [product, customizationOption, siteSettings];
